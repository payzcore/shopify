# Shopify App Credentials
# Get these from your Shopify Partner Dashboard:
# https://partners.shopify.com → Apps → Create App
SHOPIFY_API_KEY=your_shopify_api_key
SHOPIFY_API_SECRET=your_shopify_api_secret

# PayzCore Credentials
# Get these from your PayzCore dashboard:
# https://app.payzcore.com → Projects → Your Project
PAYZCORE_API_KEY=pk_live_xxx
PAYZCORE_WEBHOOK_SECRET=whsec_xxx
PAYZCORE_API_URL=https://api.payzcore.com

# App Configuration
# The public URL where this app is hosted (no trailing slash)
APP_URL=https://your-app-domain.com
PORT=3001

# Session encryption key (generate a random 64+ char string)
SESSION_SECRET=your_random_session_secret_at_least_64_characters_long

# Default blockchain network for payments
# Options: TRC20, BEP20, ERC20, POLYGON, ARBITRUM
DEFAULT_NETWORK=TRC20

# Default token for payments
# Options: USDT, USDC
DEFAULT_TOKEN=USDT

# Enabled blockchain networks (comma-separated, OPTIONAL)
# If not set, networks are fetched automatically from PayzCore API at startup.
# Set this only to override the API config with a custom subset.
# Example: ENABLED_NETWORKS=TRC20,BEP20,POLYGON
# ENABLED_NETWORKS=TRC20,BEP20,ERC20,POLYGON,ARBITRUM
