<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment Complete - <%= orderName %></title>
  <link rel="stylesheet" href="/css/payment.css">
</head>
<body>
  <div class="container">

    <div class="success-page">
      <div class="success-icon">
        <svg width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="#06b6d4" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
          <polyline points="22 4 12 14.01 9 11.01"></polyline>
        </svg>
      </div>
      <h1><%= texts.complete.title %></h1>
      <p class="success-details">
        <%= texts.complete.detail.replace('{amount}', amount).replace('{token}', token).replace('{network}', network).replace('{order}', orderName) %>
      </p>
      <% if (explorerUrl) { %>
      <p class="explorer-link">
        <a href="<%= explorerUrl %>" target="_blank" rel="noopener noreferrer">View transaction on explorer</a>
      </p>
      <% } %>
      <p class="success-note"><%= texts.complete.processing %></p>
      <a href="<%= returnUrl %>" class="btn btn-primary"><%= texts.complete.returnToStore %></a>
    </div>

  </div>
</body>
</html>
